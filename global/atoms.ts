import { atom } from "recoil";

export const locationIsSelectedState = atom({
    key:'locationIsSelectedState',
    default:false
})

export const freesInfoState = atom({
    key:'freesInfoState',
    default:false
})